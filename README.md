# f
Fedora package helper

`f` -> update packages

`f <keywords>` -> search [Fedora packages](https://packages.fedoraproject.org) with the given keywords

`f -S <package>` -> install package

`f -r <package>` -> remove package
